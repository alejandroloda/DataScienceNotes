import setuptools

setuptools.setup(
    name="loda_utils",
    version="0.0.1",
    description="Usefull and simpled functions to save time coding.",
    packages=setuptools.find_packages(),
    python_requires='>=3.8',
)
